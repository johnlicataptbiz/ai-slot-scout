# Local development example
API_KEY=your_google_ai_key_here
NODE_ENV=development

# When deploying, set API_KEY as a secret in your hosting platform (Vercel/Netlify/etc.)

# Optional Sentry settings
# Server-side DSN (for serverless handlers)
SENTRY_DSN=
# Client-side DSN for Vite (will be exposed to browser): prefix with VITE_
VITE_SENTRY_DSN=
VITE_SENTRY_ENV=development

# Redis (optional)
REDIS_URL=redis://localhost:6379
RATE_LIMIT_POINTS=10
RATE_LIMIT_DURATION=60
